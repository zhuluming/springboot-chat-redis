<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.laywerspringboot.edition.dao.CaseDao">

    <resultMap type="com.laywerspringboot.edition.entity.Case" id="CaseMap">
        <result property="caseid" column="caseId" jdbcType="VARCHAR"/>
        <result property="party" column="party" jdbcType="VARCHAR"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
        <result property="court" column="court" jdbcType="VARCHAR"/>
        <result property="region" column="region" jdbcType="VARCHAR"/>
        <result property="lawyer" column="lawyer" jdbcType="VARCHAR"/>
        <result property="admin" column="admin" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="CaseMap">
        select
          caseId, party, phone, court, region, lawyer, admin
        from myrhythm.case
        where caseId = #{caseid}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="CaseMap">
        select
          caseId, party, phone, court, region, lawyer, admin
        from myrhythm.case
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="CaseMap">
        select
        caseId, party, phone, court, region, lawyer, admin
        from myrhythm.case
        <where>
            <if test="caseid != null and caseid != ''">
                and caseId = #{caseid}
            </if>
            <if test="party != null and party != ''">
                and party = #{party}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
            <if test="court != null and court != ''">
                and court = #{court}
            </if>
            <if test="region != null and region != ''">
                and region = #{region}
            </if>
            <if test="lawyer != null and lawyer != ''">
                and lawyer = #{lawyer}
            </if>
            <if test="admin != null and admin != ''">
                and admin = #{admin}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="caseid" useGeneratedKeys="true">
        insert into myrhythm.case(party, phone, court, region, lawyer, admin)
        values (#{party}, #{phone}, #{court}, #{region}, #{lawyer}, #{admin})
    </insert>

    <insert id="insertBatch" keyProperty="caseid" useGeneratedKeys="true">
        insert into myrhythm.case(party, phone, court, region, lawyer, admin)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.party}, #{entity.phone}, #{entity.court}, #{entity.region}, #{entity.lawyer}, #{entity.admin})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="caseid" useGeneratedKeys="true">
        insert into myrhythm.case(party, phone, court, region, lawyer, admin)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.party}, #{entity.phone}, #{entity.court}, #{entity.region}, #{entity.lawyer}, #{entity.admin})
        </foreach>
        on duplicate key update
        party = values(party) , phone = values(phone) , court = values(court) , region = values(region) , lawyer =
        values(lawyer) , admin = values(admin)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update myrhythm.case
        <set>
            <if test="party != null and party != ''">
                party = #{party},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="court != null and court != ''">
                court = #{court},
            </if>
            <if test="region != null and region != ''">
                region = #{region},
            </if>
            <if test="lawyer != null and lawyer != ''">
                lawyer = #{lawyer},
            </if>
            <if test="admin != null and admin != ''">
                admin = #{admin},
            </if>
        </set>
        where caseId = #{caseid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from myrhythm.case where caseId = #{caseid}
    </delete>

</mapper>